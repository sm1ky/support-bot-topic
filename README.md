# 🤖 Support Bot [🔐]

## Описание
Бот поддержки пользователей с поддержкой топиков в группе

<details><summary><b>Возможности</b></summary>
1. Поддержка топиков

2. Управление обращениями
    >`/close - закрыть обращение`
    >`/open - открыть обращение`
    >`/closeall - закрыть все обращения` (только для администратора)

3. Каждые 3 часа в general топик группы отправляется сводка по новым обращениям
    >`/summary - получить сводку (работает только в general)`

4. BUMP топика каждые 2 часа
    >Каждые 2 часа в новые и открытые топики отправляется BUMP
    >Сделано это для того, чтобы не потерять обращение
</details>

---

## Установка

1. Скачай `docker-compose.yml` и `.env` с помощью `curl`:

    ```bash
    curl -o docker-compose.yml https://raw.githubusercontent.com/sm1ky/support-bot-topic/refs/heads/main/docker-compose.yml
    curl -o .env https://raw.githubusercontent.com/sm1ky/support-bot-topic/refs/heads/main/.env.example
    ```

3. Настрой переменные в `.env`:

    ```bash
    cp .env.example .env
    nano .env
    ```

    Заполни `BOT_TOKEN`, `GROUP_CHAT_ID` и другие параметры.

4. Запусти бота:

    ### Вариант 1: Использовать готовый образ из GitHub

    ```bash
    docker compose pull
    docker compose up -d
    ```

    ### Вариант 2: Собрать образ из локальных файлов

    Отредактируйте файл `docker-compose.yml`, раскомментировав опцию с локальной сборкой и закомментировав опцию с готовым образом:

    ```yaml
    # Опция 1: Использовать готовый образ из GitHub
    # image: ghcr.io/sm1ky/support-bot-topic:latest
    
    # Опция 2: Собрать из локальных файлов
    build:
      context: .
      dockerfile: Dockerfile
    ```

    Затем запустите:
    ```bash
    docker compose build
    docker compose up -d
    ```

    ### Вариант 3: Использовать docker-compose.local.yml для разработки

    Для разработки и отладки вы можете использовать специальный файл:
    ```bash
    docker compose -f docker-compose.local.yml up --build
    ```
    Этот вариант монтирует текущую директорию в контейнер, что позволяет видеть изменения в коде без пересборки.

## Переменные окружения

| Переменная         | Описание                                  | Пример значения          |
|--------------------|-------------------------------------------|--------------------------|
| `BOT_DEV_ID`       | ID администратора бота                    | `1927996831`             |
| `BOT_EMOJI_ID`     | Кастомный emoji (опционально)             | (пусто или ID эмодзи)    |
| `REDIS_HOST`       | Хост Redis                                | `redis`                  |
| `REDIS_PORT`       | Порт Redis                                | `6379`                   |
| `REDIS_DB`         | БД Redis                                  | `0`                      |
| `BOT_TOKEN`        | Токен бота (получи у @BotFather)          | `***********`            |
| `BOT_GROUP_ID`     | ID группы                                 | `-100*******`            |
| `BOT_USERNAME`     | Юзернейм бота (без @)                     | `YOUR_BOT_USERNAME`      |
| `BOT_NAME`         | Название проекта / бота                     | `My Project name`      |



---

### Fork
**Based on [nessshon/support-bot](https://github.com/nessshon/support-bot)**
